import React from 'react';
import NexusHomepage from '@/components/home/NexusHomepage';

import Footer from '@/components/home/Footer';

export const metadata = {
  title: 'NEXUS — The Modern Standard for Pakistani Events',
  description: 'Plan events. Run businesses. Manage venues. Preserve memories. Power experiences — all in one unified platform.',
};

export default function MarketingPage() {
  return (
    <NexusHomepage />
  );
}
